const getNativeHook = () =>
  ((window.webkit && window.webkit.messageHandlers
    && window.webkit.messageHandlers.nativeWebViewAction) ?
    window.webkit.messageHandlers.nativeWebViewAction :
    window.nativeWebViewAction);

export default getNativeHook;
