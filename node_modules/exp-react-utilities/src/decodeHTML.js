const decodeHTML = (string) => {
  if (!string) return ''; // Don't return an "undefined" string
  const div = document.createElement('div');
  div.innerHTML = string;
  return div.textContent;
};

export default decodeHTML;
